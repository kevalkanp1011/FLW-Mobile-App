<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.home_activity.service_type.ServiceTypeFragment">

    <data>
        <variable
            name="viewModel"
            type="org.piramalswasthya.sakhi.ui.home_activity.service_type.ServiceTypeViewModel" />
    </data>


    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorPrimary"
        >

        <androidx.cardview.widget.CardView
            android:id="@+id/cv_login_item_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="@dimen/margin_normal"
            android:elevation="@dimen/padding_wide"
            app:cardCornerRadius="@dimen/padding_wide"
            app:cardElevation="@dimen/padding_wide">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/padding_wide">

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/tv_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingTop="@dimen/padding_normal"
                    android:paddingBottom="@dimen/padding_normal"
                    android:text="@{@string/tv_text_welcome(viewModel.userName)}"
                    android:textAppearance="@style/TextAppearance.Material3.TitleLarge" />

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/tv_select_service_region"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingTop="@dimen/padding_normal"
                    android:paddingBottom="@dimen/padding_normal"
                    android:text="@string/tv_text_select_language"
                    android:textAppearance="@style/TextAppearance.Material3.TitleLarge" />

                <include
                    android:id="@+id/dd_state"
                    layout="@layout/rv_item_dropdown"
                    app:field="@{@string/service_type_dd_state_text}"
                    app:listItems="@{viewModel.stateList}"
                    app:required="@{true}"

                    />
                <include
                    android:id="@+id/dd_district"
                    layout="@layout/rv_item_dropdown"
                    app:field="@{@string/service_type_dd_district_text}"
                    app:listItems="@{viewModel.districtList}"
                    app:required="@{true}"

                    />
                <include
                    android:id="@+id/dd_block"
                    layout="@layout/rv_item_dropdown"
                    app:field="@{@string/service_type_dd_block_text}"
                    app:listItems="@{viewModel.blockList}"
                    app:required="@{true}"

                    />
                <include
                    android:id="@+id/dd_village"
                    layout="@layout/rv_item_dropdown"
                    app:field="@{@string/service_type_dd_village_text}"
                    app:listItems="@{viewModel.villageList}"
                    app:required="@{true}"

                    />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_continue"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_margin="@dimen/padding_normal"
                    android:padding="@dimen/padding_normal"
                    android:text="@string/btn_continue_text"
                    android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
                    android:textColor="?attr/colorSecondary" />
            </LinearLayout>

        </androidx.cardview.widget.CardView>


    </FrameLayout>
</layout>